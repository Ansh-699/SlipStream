# S3 — Margin, funding, liquidation, triggers

**Run:** `audit-e2e` · **Issue:** #5 · **Freeze SHA:** `b98f0508b01af619f84a6cbb82b6326ebbcd7f9f`

Method: read all 14 owned files in full, plus every caller and every writer of the
state they touch (`crank_twap`, `settle_trades`, `place_order`, `withdraw_collateral`,
`initialize_market`, `oracle.rs`). Ran `cargo test --workspace` (123 pass: 25 program-crate
+ 98 Mollusk, after `cargo build-sbf --manifest-path programs/slipstream/Cargo.toml`).
Took read-only devnet reads of the market, the Pyth and Switchboard feed accounts, the
quote vault, and all 34 program-owned 96-byte accounts. Reproductions and the raw account
snapshots are under `docs/audit/audit-e2e/repro/s3/`.

**Producer contract.** `S5-C7` was re-derived independently here from source
(`state/market.rs`, `instructions/crank_twap.rs`, `oracle.rs`) and from the live market
account, before S5's block was read. S5's published contract
(`docs/audit/audit-e2e/s5.md:60-90`) agrees on every load-bearing point — single writer,
stamp semantics, the 45.5-day aliasing bound, and the gate holding live — so there is no
producer/consumer disagreement to file. One consumer-side strengthening of S5's item 6:
the `stamp == 0` hole is not merely absent from the live market but **unreachable with a
non-zero price**, because `crank_twap.rs:71-76` writes the price and the stamp on adjacent
lines and `initialize_market.rs:178-181` zeroes both together (see the S3-C13 bullet).
This slice finds the `mark_price_for_close` gate itself sound and *correctly firing*
today; the defects are in the price consumers that do not go through it.

## Calibration

> Flag only gaps that affect correctness, the stated requirements, or documented project invariants -- cite file:line evidence for every finding. Do not report stylistic preferences.

## Findings

### S3-01 · One permissionless call multiplies an uncapped funding premium by 48 uncapped catch-up intervals

- **Status:** CONFIRMED
- **Severity:** P0
- **Tag:** [reachable-now]
- **Class:** S3-C4
- **Evidence:** `programs/slipstream/src/instructions/compute_funding.rs:19-46`, `programs/slipstream/src/instructions/compute_funding.rs:70-80`, `programs/slipstream/src/math/funding.rs:15-34`, `programs/slipstream/src/instructions/mod.rs:120`, `programs/slipstream/src/instructions/claim_funding.rs:99-104`

**What is wrong.** `compute_funding` takes the *instantaneous* premium
`(mark - index) / index` and multiplies it by `intervals`, the number of whole funding
intervals that elapsed since `last_funding_ts` — and neither factor has a bound. The
premium is an unclamped ratio (`funding.rs:23-26`); `intervals` is an unclamped
`elapsed / interval_secs` (`compute_funding.rs:41`). Every real perpetual clamps the
per-interval funding rate for exactly this reason, and the invariant is written into the
program's own units: `INTEREST_RATE_PER_INTERVAL` is documented at `funding.rs:5-7` as
"0.01% = 1 bps", the scale the index is meant to move at. There is no signer check
anywhere on the path — `mod.rs:120` dispatches straight into `process`, which requires
only that `market_acc` be program-owned (`compute_funding.rs:23-25`) — so any devnet
account holding tx-fee lamports can fire it. The mechanism that hid it: the one
regression test for this code (`tests/unit/src/test_compute_funding_regressions.rs:109`)
deliberately sets `twap_prices[0] = price` so mark == index and the premium is zero,
"to isolate interval scaling". The multiplier is therefore only ever exercised against a
rate of zero.

**Proof.** Live devnet market `ECUp8pXzVLzxjVs8mtKBJma3mdcHf8zSC4cqPeBy8MPy` read at
2026-08-21T19:13:46Z (snapshot and replay in `docs/audit/audit-e2e/repro/s3/`):

```
$ python3 docs/audit/audit-e2e/repro/s3/funding_catchup.py
=== one permissionless compute_funding call (disc 0x06) ===
  elapsed              1,401,026 s (16.22 days)
  intervals            48                <- UNCAPPED (compute_funding.rs:41)
  premium_rate         -189,297,877,255,452,698  (-18.929788% per interval)   <- UNCAPPED (funding.rs:23-26)
  rate_per_interval    -189,197,877,255,452,698  (-18.919788%)
  x 48 intervals      -9,081,498,108,261,729,504  (-908.1498% of notional)
  new cumulative index -9,120,169,729,644,858,361  (-9.120170)

=== what each side then claims (claim_funding.rs:66-104) ===
  1 SOL notional at the fresh mark: $91.32
  long  1 SOL -> payment       -829,303,018  => CREDITED $829.30  (claim_funding.rs:99-104)
  short 1 SOL -> payment        829,303,018  => DEBITED  $829.30  (claim_funding.rs:88-98)
  credit is 9.1x the position's own notional
```

Preconditions all hold on the live deployment today: `twap_count = 225` so
`get_twap()` is `Some` (`compute_funding.rs:51`); Switchboard's `result.value` is `0`,
so `oracle.rs:262-271` takes the Pyth-only fallback; the Pyth feed
`7UVimffxr9ow1uXYxsr4LHAcV58mLzhmwaeKvJ1pjLiE` published 44 s before the read, inside
the 60 s `MAX_STALENESS_SECS`, so `apply_dual_oracle` returns `Price(91_317_865)`.
Applied to the actual 26 open positions:

```
$ python3 docs/audit/audit-e2e/repro/s3/live_positions.py
=== after one permissionless compute_funding call (S3-01) ===
  LONG  side  collateral $   20,700.62   is CREDITED $   72,322.04
  SHORT side  collateral $   29,705.36   OWES        $   78,013.17
  collectable from shorts (capped by their collateral):  $   29,705.36
  SILENTLY FORGIVEN by saturating_sub, claim_funding.rs:97: $ 48,307.81
  NET new withdrawable claim minted on the vault:        $   42,616.68
  vault actually holds                                   $  655,915.85
  insurance_fund_balance                                 $       20.73
```

**Blast radius.** Every position holder on the market, and every depositor whose USDC
sits in the shared quote vault `BTWVG5oDmomaMkpdX4xYgfjr1gpEGodW5oPe3k5P71qr`. The
credited side's `free_collateral` is withdrawable with no vault-solvency check —
`withdraw_collateral.rs:86` gates only on `user.free_collateral < amount` — so the
$42,616.68 of net new claim is a direct drain path against a $655,915.85 vault backed by
a $20.73 insurance fund. The attacker needs no privilege, no position of their own to
fire the instruction, and roughly 5,000 lamports; they profit by holding the long side
first. The precondition (a large `intervals` count, or a large mark/index gap) is
recreated by any keeper outage, and K1 says outages happen.

**Suggested remediation.** Clamp the per-interval funding rate to a configured maximum
(e.g. ±75 bps) inside `compute_funding_rate`, and cap `intervals` at a small constant
(1–3) in `compute_funding` so a catch-up can never apply more than a bounded multiple of
that clamp in one call. Both bounds belong in the program, not in the keeper, because the
instruction is permissionless.

### S3-02 · `compute_funding` prices the mark off a raw `get_twap()`, the one price read `mark_price_for_close` does not cover

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** S3-C13
- **Evidence:** `programs/slipstream/src/instructions/compute_funding.rs:48-52`, `programs/slipstream/src/state/market.rs:163-183`, `programs/slipstream/src/state/market.rs:12-16`, `programs/slipstream/src/instructions/settle_trades.rs:235`, `programs/slipstream/src/instructions/settle_trades.rs:275`

**What is wrong.** `Market::mark_price_for_close` exists precisely so that a dead crank
cannot silently price a settlement, and all three of its callers use it
(`close_position.rs:123-125`, `claim_funding.rs:61-63`, `execute_trigger.rs:81-83`).
`compute_funding` is a fourth price consumer and it bypasses the gate entirely: it calls
`market.get_twap()` directly (`compute_funding.rs:51`), which returns the mean of the
`twap_prices` ring with no timestamp attached and no freshness test of any kind. The
funding index it writes is then consumed by all three gated callers, so the gate protects
the *settlement* price while leaving the *rate* it settles against sourced from a
16-day-old book. Secondary, in the same gate: the doc comment at `market.rs:12-16`
justifies the generous 30-minute window with "every settled fill also refreshes it" —
that refresh path does not exist. `settle_trades.rs:275` only *reads* `last_mark_price`,
and `settle_trades.rs:235` explicitly forbids sourcing it from a fill price. `crank_twap`
is the only writer, so the real budget is 225 missed cranks from one process, not two
independent refreshers.

**Proof.** The gate itself is sound and is firing correctly today — that is the negative
result that makes the asymmetry legible:

```
$ python3 docs/audit/audit-e2e/repro/s3/funding_catchup.py
=== Market::is_mark_price_fresh (state/market.rs:154-161) ===
  now_min 35649  stamp 12436  wrapping_sub 23213 min (16.1 days)  fresh=False
  => mark_price_for_close() returns None (state/market.rs:173-183)
  last_mark_price                    74,114,120  ($74.114120)
  get_twap()                         74,031,587  ($74.031587)
  pyth index price                   91,317,865  ($91.317865)
```

So on the same account, in the same block: the three gated callers refuse to run, while
`compute_funding` reads `$74.031587` as a live mark and prices it against a `$91.317865`
index — a -18.93% premium that is an artefact of the outage, not of the market. A grep of
every `last_mark_price` / `set_mark_price_minute` write in the program confirms the single
writer and confirms no path can leave the price set with the stamp cleared:

```
$ grep -rn "last_mark_price *=\|set_mark_price_minute" --include='*.rs' programs/
programs/slipstream/src/instructions/initialize_market.rs:178:    market.last_mark_price = 0;
programs/slipstream/src/instructions/crank_twap.rs:72:    market.last_mark_price = price;
programs/slipstream/src/instructions/crank_twap.rs:76:    market.set_mark_price_minute(now_min);
```

**Blast radius.** Everyone holding a position when a crank outage ends. This is the
delivery mechanism for S3-01's magnitude: without the un-gated TWAP read the premium
would be a normal basis, and the 48× multiplier would move the index by tens of basis
points instead of 908%. It also mis-prices funding continuously in the ordinary case,
because the TWAP is a 30-minute lagging average of the *local book* midprice while the
index is a spot oracle read.

**Suggested remediation.** Route `compute_funding`'s mark through
`mark_price_for_close(now)` (or an equivalent freshness-stamped read) and return
`OracleStale` when it is `None`, so funding cannot accrue against a dead feed. Separately,
correct the `market.rs:12-16` comment to name `crank_twap` as the only refresher, since
the staleness budget was sized on the belief that there were two.

### S3-03 · The stale-mark gate is one-sided: liquidation runs off the live oracle while the owner's exit paths are hard-blocked

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** S3-C6
- **Evidence:** `programs/slipstream/src/instructions/liquidate_position.rs:89-101`, `programs/slipstream/src/instructions/liquidate_position.rs:126-163`, `programs/slipstream/src/instructions/close_position.rs:119-125`, `programs/slipstream/src/instructions/claim_funding.rs:59-63`, `programs/slipstream/src/instructions/execute_trigger.rs:78-86`, `PRODUCT.md:51`

**What is wrong.** The health factor that authorises a liquidation is computed from
`apply_dual_oracle`'s **live** reading (`liquidate_position.rs:91-101`, feeding
`compute_notional`/`compute_unrealized_pnl`/`compute_health_factor` at lines 132-155).
Every path by which the *owner* can act on the same position — `close_position`,
`claim_funding`, `execute_trigger`'s stop-loss — is gated on `mark_price_for_close`, which
is `None` whenever the crank is dead. The two prices are therefore allowed to diverge
without limit, and the divergence is one-directional: the protocol can seize a position at
a price the owner is forbidden from transacting at, including a stop-loss the owner placed
in advance for exactly this scenario. No document states this asymmetry; `PRODUCT.md:51`
lists "partial close and slippage-bounded close-at-market" and "keeper-executed stop-loss
/ take-profit triggers" as *live capabilities*, and both are hard-down on the deployment
today.

**Proof.** Both halves verified against the same live account snapshot:

```
$ python3 docs/audit/audit-e2e/repro/s3/live_positions.py
=== liquidate_position health at the live oracle ($91.317865) ===
  8hgAVi6dWYZjFGnokfBU4prJGAxRhrVSaBfUuAM4M64f  size   -0.600 SOL  collateral $    5.71  uPnL $    -5.83  health 0.947
  DrVL7kqFS5kSqN54v3Ris94omj6uf3zi1mv3NFNvFSwK  size   -0.400 SOL  collateral $   20.00  uPnL $   -36.13  health 0.000
  => 2 of 26 positions are liquidatable NOW.
     Their owners cannot call close_position at all: mark_price_for_close()
     returns None (state/market.rs:173-183) so it errors OracleStale.
```

`liquidate_position` has no dependency on `mark_price_for_close` anywhere in its 363
lines, and its remaining gates are all open on the live market: `circuit_breaker_active`
is `0`, `restricted_mode` is `0`, and the Pyth-only fallback at `oracle.rs:262-271`
returns `Price(..)` because Switchboard's `result.value` is `0` and Pyth is 44 s fresh.

**Blast radius.** The two positions above today, and every position during any future
crank outage. A trader who did everything right — sized conservatively, placed a stop —
is liquidated at the oracle price while their own stop-loss returns `OracleStale`, and
they eat the loss plus S3-04's uncollected bounty deduction. On mainnet this is the
classic "exchange halts the exit but not the liquidator" failure; it converts a keeper
outage into realised trader losses.

**Suggested remediation.** Make the two sides share one price source. Either allow the
owner-initiated close paths to fall back to the same `apply_dual_oracle` reading
`liquidate_position` already uses (passing the feed accounts in), or gate
`liquidate_position` on mark freshness too so that a dead crank freezes both directions.
Freezing both is the safer default; freezing only the exit is the one combination that
should not ship.

### S3-04 · The liquidator bounty is deducted from the trader and paid to nobody

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** S3-C8
- **Evidence:** `programs/slipstream/src/instructions/liquidate_position.rs:187-193`, `programs/slipstream/src/instructions/liquidate_position.rs:203-237`, `programs/slipstream/src/instructions/liquidate_position.rs:337-362`, `tests/unit/src/test_liquidate_position_regressions.rs:58`, `PRODUCT.md:51`

**What is wrong.** Lines 187-193 compute `liquidator_bonus` and the comment states the
invariant: "Compute liquidator bonus: 50bps of notional, capped at 20% of the position's
remaining net collateral, per §16." The value is then used on exactly one further line —
`- (liquidator_bonus as i128)` at line 206 — which reduces `total_settlement`, i.e.
reduces what the liquidated trader is credited at line 222-226. Nothing ever credits the
liquidator with it, and nothing adds it to `insurance_fund_balance` either (the `else`
branch at 227-237 only *subtracts* from the fund). The liquidator's entire compensation is
the `LiquidationIntent` rent moved at lines 356-361, which only exists on the
`pending_fills > 0` path and is a few thousand lamports of SOL, not a fee proportional to
the risk taken. So the bounty is *charged* and *destroyed*: the USDC stays in the vault
with no `free_collateral` claim against it.

**Proof.** Every reference to the value in the entire program:

```
$ grep -rn "liquidator_bonus" --include='*.rs' programs/ tests/
programs/slipstream/src/instructions/liquidate_position.rs:193:    let liquidator_bonus = bonus_bps.min(bonus_pct);
programs/slipstream/src/instructions/liquidate_position.rs:206:        - (liquidator_bonus as i128);
```

Two references, neither of which pays anybody. Worked example replayed from the program's
own arithmetic:

```
$ python3 docs/audit/audit-e2e/repro/s3/funding_catchup.py
=== liquidator bonus (liquidate_position.rs:187-237) ===
  notional $900.00  50bps=$4.50  20% of net=$1.00  -> bonus $1.00
  trader credited $4.00 (bonus deducted, line 206)
  liquidator credited: $0.00 -- `liquidator_bonus` is referenced on exactly
  two lines of the file (193, 206) and neither pays anybody.
```

The mechanism that hid it: the two Mollusk tests over this instruction
(`tests/unit/src/test_liquidate_position_regressions.rs:58` and `:185`) cover a forged
intent account and the healthy-position early return; neither reaches the settlement
branch, and no test in the repo asserts on a liquidator's balance. The only prose
describing the payout lives in `tests/integration/`, which K8 records as never run.

**Blast radius.** Two distinct harms. (1) Every liquidated trader is over-charged by the
bonus amount — up to 20% of their remaining net collateral, since `bonus_pct` at line 192
is `net_collateral / 5` and is usually the binding cap on an underwater position. (2) The
liquidation incentive does not exist, and it degrades exactly where it matters: when
`net_collateral` reaches 0 the cap makes the bonus 0 as well, so nobody is paid to clear
the positions that generate bad debt, and the $20.73 insurance fund absorbs it. On mainnet
this means liquidations only happen while the operator's own keeper is alive — which K1
shows is not a safe assumption.

**Suggested remediation.** Credit `liquidator_bonus` to the liquidator's `UserAccount`
(add the account to the instruction and `checked_add` it into `free_collateral`) in the
same branch that deducts it, and add a floor so the bounty stays non-zero on an underwater
position — funded from the insurance fund rather than from collateral that is already
gone. Add a Mollusk test that asserts the liquidator's balance changed.

### S3-05 · `claim_funding` forgives an uncollectable funding debit while crediting the other side unconditionally

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [mainnet-only]
- **Class:** S3-C3
- **Evidence:** `programs/slipstream/src/instructions/claim_funding.rs:88-104`, `programs/slipstream/src/instructions/close_position.rs:232-252`, `programs/slipstream/src/instructions/withdraw_collateral.rs:86`

**What is wrong.** The paying side's debit is applied with `saturating_sub`
(`claim_funding.rs:97`): if `free_collateral` plus the position's collateral is less than
what is owed, the shortfall is silently dropped — no `insurance_fund_balance` debit, no
log, no error, and the position's funding snapshot has already been advanced at line 75,
so the debt cannot be re-collected later. The receiving side's credit at lines 100-103 is
unconditional `checked_add` into `free_collateral` with no counterparty and no source
account. Funding is therefore not conservative: the credits are minted, the debits are
capped, and the difference is a claim on the shared vault. The program itself shows it
knows this matters — the equivalent deficit in `close_position.rs:232-252` is debited from
the insurance fund *and* logged with `sol_log_64` because "a silent one is unauditable".
`claim_funding` does neither. `withdraw_collateral.rs:86` gates a withdrawal only on
`user.free_collateral < amount`, so a minted credit is directly withdrawable.

**Proof.** Every write to a balance in `claim_funding.rs`, showing there is no third
account and no fund debit anywhere in the file:

```
$ grep -n "free_collateral\|collateral =\|insurance" programs/slipstream/src/instructions/claim_funding.rs
90:        if user_mut.free_collateral >= abs_payment {
91:            user_mut.free_collateral -= abs_payment;
94:            let shortfall = abs_payment - user_mut.free_collateral;
95:            user_mut.free_collateral = 0;
97:            pos_mut2.collateral = pos_mut2.collateral.saturating_sub(shortfall);
100:        user_mut.free_collateral = user_mut
101:            .free_collateral
102:            .checked_add((-payment) as u64)
```

Replayed against the live position set, using the index S3-01 can install today:

```
$ python3 docs/audit/audit-e2e/repro/s3/live_positions.py
  SHORT side  collateral $   29,705.36   OWES        $   78,013.17
  collectable from shorts (capped by their collateral):  $   29,705.36
  SILENTLY FORGIVEN by saturating_sub, claim_funding.rs:97: $ 48,307.81
```

Tagged `[mainnet-only]` because `claim_funding` currently reverts before reaching this
code — its own `mark_price_for_close` gate at line 61-63 returns `OracleStale` (S3-03). It
becomes reachable on devnet the moment a single `crank_twap` runs, which is a keeper
restart away.

**Blast radius.** Every depositor in the shared quote vault. In the ordinary case the leak
is small and one-sided (an underwater payer's residue), but it is unbounded in principle
because nothing ties the credited total to the collected total, and S3-01 shows the
magnitude a single call can reach. The cost lands on whoever withdraws last.

**Suggested remediation.** Make the funding transfer conservative: debit the shortfall
from `insurance_fund_balance` (and log it, as `close_position` does) when the payer cannot
cover, and refuse to credit the receiving side beyond what the market can actually source.
A per-market funding pool that credits are drawn from is the structural fix; the insurance
debit plus a log is the minimum.

### S3-06 · `cancel_trigger` and `execute_trigger` act on accounts they never bind

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S3-C10
- **Evidence:** `programs/slipstream/src/instructions/cancel_trigger.rs:30-35`, `programs/slipstream/src/instructions/cancel_trigger.rs:40-53`, `programs/slipstream/src/instructions/execute_trigger.rs:74-86`, `programs/slipstream/src/instructions/liquidate_position.rs:321-349`

**What is wrong.** Two missing bindings, both currently constrained by something else.
(1) `cancel_trigger` loads the trigger with `TriggerOrder::from_account_info`, which checks
only `data[0] == DISC_TRIGGER_ORDER` (`trigger_order.rs:38-47`), then compares
`trigger.owner` to the signer and hands the account to `close_trigger_account`, which
zeroes all of its data and drains all of its lamports. It never checks
`trigger_acc.owner() == program_id` and never re-derives the
`["trigger", owner, market_index, kind]` PDA — unlike `execute_trigger.rs:51-59`, which
does re-derive, and unlike `close_liquidation_intent`, whose 25-line comment block at
`liquidate_position.rs:321-336` exists specifically because that same pair of checks was
missing there and was added. The sibling close path was hardened; this one was not.
(2) `execute_trigger` evaluates the whole trigger condition — `mark_price_for_close` at
line 82 and `is_met` at line 84 — against a `market_acc` whose owner and PDA have not been
checked; that validation happens later, inside `do_close` (`close_position.rs:83-93`).

**Proof.** The absent checks, by contrast with the two places the same program does
perform them:

```
$ grep -n "owner()\|find_program_address" programs/slipstream/src/instructions/cancel_trigger.rs
$ grep -n "owner()\|find_program_address" programs/slipstream/src/instructions/execute_trigger.rs
53:    let (expected_pda, _bump) = pinocchio::pubkey::find_program_address(
$ grep -n "owner()\|find_program_address" programs/slipstream/src/instructions/liquidate_position.rs
67:    if market_acc.owner() != program_id {
72:        let (market_pda, _) = pinocchio::pubkey::find_program_address(
269:    let (expected_pda, bump) = pinocchio::pubkey::find_program_address(
343:    if intent_acc.owner() != program_id {
```

`cancel_trigger.rs` produces zero matches: no ownership assertion and no PDA derivation
anywhere in the file. Neither gap is exploitable today — the SVM refuses a lamport or data
write to an account this program does not own, and `do_close` rejects a substituted market
before any state changes — which is why this is P2 and not higher.

**Blast radius.** No exploit path at the current instruction set. The exposure is that
`close_trigger_account` is a general-purpose "zero this account and take its lamports"
primitive reachable with one discriminator byte and one 32-byte field match; any future
program-owned account type that shares byte 0 with `DISC_TRIGGER_ORDER`, or any future
caller that passes a non-PDA account, turns it into the bug that was already fixed once at
`liquidate_position.rs:337-349`.

**Suggested remediation.** In `cancel_trigger`, add `trigger_acc.owner() != program_id`
and re-derive the trigger PDA from `trigger.market_index` / `trigger.kind` before calling
`close_trigger_account` — the same three checks `close_liquidation_intent` settled on. In
`execute_trigger`, hoist the `market_acc.owner()` and market-PDA checks above the
`mark_price_for_close` read rather than relying on `do_close` to catch it afterwards.

### S3-07 · `compute_funding` divides by `funding_interval_secs`, which is never validated as non-zero

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S3-C12
- **Evidence:** `programs/slipstream/src/instructions/compute_funding.rs:34-46`, `programs/slipstream/src/instructions/initialize_market.rs:85-92`, `programs/slipstream/src/instructions/initialize_market.rs:173`, `programs/slipstream/src/math/fixed_point.rs:65-70`

**What is wrong.** `compute_funding.rs:41` computes `elapsed / interval_secs` and line 45
computes `intervals * interval_secs`, both on `market.funding_interval_secs` taken
straight from the account. `initialize_market` validates `tick_size`, `lot_size`,
`max_leverage` (`initialize_market.rs:85-87`) and the fee pair
(`initialize_market.rs:91-92`), but writes `funding_interval` unchecked at line 173. With
`funding_interval_secs == 0`, the guard at `compute_funding.rs:38` (`elapsed < 0`) is
false for any `now >= last_funding_ts`, execution reaches line 41, and integer division by
zero panics — which in SBF is an unrecoverable program abort, not a returned error. Funding
would then be permanently unaccruable on that market with no way to fix it short of a
program upgrade, since nothing else can write the field. Every other divisor in this
slice's math is guarded: `div_price` at `fixed_point.rs:34`, `compute_initial_margin` at
`fixed_point.rs:66-68`, `compute_funding_rate` at `funding.rs:16-18`, `compute_twap` at
`funding.rs:85-87`. `funding_interval_secs` is the one that is not.

**Proof.** The validation block, complete, alongside the field it does not cover:

```
$ sed -n '85,92p;173p' programs/slipstream/src/instructions/initialize_market.rs
    if tick_size == 0 || lot_size == 0 || max_leverage == 0 {
        return Err(SlipstreamError::InvalidMarketParams.into());
    }
    // maker_rebate is paid out of the taker fee on every fill (settle_trades.rs /
    // settle_from_log.rs). A rebate above the fee pays out more than was collected.
    if taker_fee_bps > 10_000 || maker_rebate_bps > taker_fee_bps {
        return Err(SlipstreamError::InvalidMarketParams.into());
    }
    market.funding_interval_secs = funding_interval;
```

Not reachable on the live market, whose `funding_interval_secs` reads `28800` in the
account snapshot under `docs/audit/audit-e2e/repro/s3/` — hence P2, an absent bound rather
than a live defect. The *check* half is routed to S1 as S3-X02; the math consequence stays
here per the slice brief.

**Blast radius.** Any market created with a zero interval, by an operator typo or by a
future permissionless market-creation path. That market's funding is bricked permanently
and every `compute_funding` call against it burns the caller's fee on an aborted
transaction.

**Suggested remediation.** Add `funding_interval == 0` to the `initialize_market` reject
list at line 85, and defensively return `DivisionByZero` from `compute_funding` when
`interval_secs == 0`, matching how `compute_initial_margin` handles `max_leverage == 0`.

### S3-08 · Three unguarded numeric edges in the shared math (batched)

- **Status:** CONFIRMED
- **Severity:** P3
- **Class:** S3-C2
- **Evidence:** `programs/slipstream/src/math/fixed_point.rs:108-113`, `programs/slipstream/src/math/fixed_point.rs:123-125`, `programs/slipstream/src/instructions/close_position.rs:178-183`, `programs/slipstream/src/instructions/liquidate_position.rs:196-201`, `programs/slipstream/src/math/funding.rs:79`

**What is wrong.** Three places where this slice's math silently produces a value instead
of refusing. (1) `compute_unrealized_pnl` ends `Ok(pnl as i64)` at `fixed_point.rs:112` —
a truncating cast on an `i128`. It is the only function in the file without a post-division
bound check; `mul_price`, `div_price`, `compute_notional` and `apply_bps` all test
`> u64::MAX` first (lines 25, 41, 58, 83), and `compute_funding_payment` uses
`i64::try_from` for the identical narrowing at `funding.rs:79`. The dedicated regression
test at `fixed_point.rs:216-223` was written for exactly this gap in `apply_bps` and the
sibling was left. (2) `compute_health_factor` returns `u64::MAX` when
`maintenance_margin == 0` (`fixed_point.rs:123-125`), i.e. "infinitely healthy" — and the
maintenance margin is a doubly-floored quantity (`notional / max_leverage / 2`), so it
reaches 0 for any dust position rather than only for an empty one. (3) Both close paths
reduce open interest with `saturating_sub` (`close_position.rs:180`, `:182`;
`liquidate_position.rs:198`, `:200`), so if the counters ever fall behind the position set
the clamp hides it permanently instead of aborting.

**Proof.** Reachability of (1) is bounded, which is what holds this at P3: `pnl =
price_diff * |size| / 1e9` exceeds `i64::MAX` only when `|price_diff| * |size| > 9.2e27`,
i.e. a ~$1,000 price move on a position of ~9.2e9 SOL, far above any size the margin
gates permit. (2) and (3) are checked against the live account and are currently clean —
the stored counters agree exactly with the actual position set:

```
$ python3 docs/audit/audit-e2e/repro/s3/live_positions.py
non-empty positions: 26
  summed long      87.400 SOL   (Market.open_interest_long  87.400)
  summed short     94.300 SOL   (Market.open_interest_short 94.300)
```

**Blast radius.** None today on any of the three; this is a hygiene record. (1) would
report an inverted PnL sign rather than aborting if it were ever reached. (2) makes a dust
position unliquidatable, which is correct-by-accident (it is also worth nothing). (3)
converts a future accounting bug elsewhere from a loud abort into a silently wrong number
on the market's headline open-interest figure.

**Suggested remediation.** Replace `pnl as i64` with `i64::try_from(pnl)` mapped to
`MathOverflow`, matching `funding.rs:79`. Leave the `u64::MAX` health branch but assert
the position is empty when the maintenance margin is 0, or floor the maintenance margin at
1 atom. Replace the four OI `saturating_sub` calls with `checked_sub` and a
`MathUnderflow` error, so a divergence surfaces at the point it is created.

### S3-09 · A short `close_position` payload is silently reinterpreted as an unbounded full close

- **Status:** CONFIRMED
- **Severity:** P3
- **Class:** S3-C11
- **Evidence:** `programs/slipstream/src/instructions/close_position.rs:47-54`, `programs/slipstream/src/instructions/close_position.rs:127-138`, `client/src/instructions.ts:816-825`

**What is wrong.** `close_position` reads its two optional fields only when
`data.len() >= 16` and otherwise substitutes `(0, 0)` — which means "close everything, no
slippage bound". Any payload of 1 to 15 bytes is accepted and reinterpreted rather than
rejected: a caller who encoded `close_size` but truncated `limit_price`, or wrote the
fields as `u32`, gets a full close with the price protection silently removed. The bound
itself is enforced correctly when it is present (lines 129-138: a long requires
`mark >= limit`, a short `mark <= limit`, checked against the same `mark_price` the
settlement uses at line 154), and `0` disabling it is documented at lines 22-27, so the
defect is only the silent widening. `PRODUCT.md:51` advertises "partial close and
slippage-bounded close-at-market" as a capability, and this is the one input shape where
the program delivers neither without saying so.

**Proof.** The in-tree SDK only ever emits the 1-byte or 17-byte form, which is why
nothing has hit this:

```
$ sed -n '816,825p' client/src/instructions.ts
  let data: Buffer;
  if (opts && (opts.closeSize !== undefined || opts.limitPrice !== undefined)) {
    data = Buffer.alloc(17);
    data[0] = IX_CLOSE_POSITION;
    data.writeBigUInt64LE(opts.closeSize ?? 0n, 1);
    data.writeBigUInt64LE(opts.limitPrice ?? 0n, 9);
  } else {
    data = Buffer.alloc(1);
    data[0] = IX_CLOSE_POSITION;
  }
```

The dispatcher strips the discriminator (`instructions/mod.rs:108-110`), so the program
sees 0 or 16 bytes from this SDK and never the intermediate lengths.

**Blast radius.** A third-party or hand-rolled client. The user believes they submitted a
partial close with a floor and receives a full close at whatever the mark happens to be —
the loss is the difference between their bound and the fill, on their whole position
rather than the fraction they asked to close.

**Suggested remediation.** Reject `1 <= data.len() < 16` with
`ProgramError::InvalidInstructionData`, keeping the `data.is_empty()` legacy path as the
only implicit "full close, no bound".

## Cross-slice notes

### S3-X01 · The global pause does not cover liquidation or funding, contradicting its own contract

- **Routed to:** S1
- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S1-C10
- **Evidence:** `programs/slipstream/src/instructions/mod.rs:169-175`, `programs/slipstream/src/instructions/liquidate_position.rs:81-87`, `programs/slipstream/src/instructions/compute_funding.rs:23-25`, `programs/slipstream/src/instructions/claim_funding.rs:32-34`, `programs/slipstream/src/instructions/close_position.rs:83-103`

**What is wrong.** The helper's own doc comment at `mod.rs:169-170` states the invariant:
"Non-trivial trading instructions (place/cancel/settle/liquidate/funding) call this before
mutating state." Neither `liquidate_position`, `compute_funding`, `claim_funding` nor
`close_position` calls it — none of them even takes a `GlobalState` account. The kill
switch therefore cannot stop the two instructions most likely to need stopping during an
oracle incident, which is when an operator would reach for it. `liquidate_position` gates
on `circuit_breaker_active` (line 84) and `close_position` on the same flag (line 101),
but that is a price-integrity auto-pause, not the operator switch.

**Proof.** Every call site in the program:

```
$ grep -rn "ensure_not_globally_paused" --include='*.rs' programs/ | grep -v "mod.rs:1"
programs/slipstream/src/instructions/place_order.rs:79
programs/slipstream/src/instructions/place_trigger.rs:57
programs/slipstream/src/instructions/deposit_collateral.rs:41
programs/slipstream/src/instructions/fund_trading_credit.rs:38
programs/slipstream/src/instructions/settle_trades.rs:81
programs/slipstream/src/instructions/settle_from_log.rs:60
programs/slipstream/src/instructions/delegate_trading_credit.rs:107
```

Seven call sites; `liquidate_position`, `compute_funding`, `claim_funding`,
`close_position`, `execute_trigger` and `cancel_order` are absent. The comment names
"liquidate" and "funding" explicitly.

**Blast radius.** An operator who pauses during an oracle incident still has liquidations
firing and a permissionless `compute_funding` accepting calls — the exact combination
S3-01 exploits. Filed at P2 because pausing is an operator action and no third party gains
anything, but the defence advertised in the code does not exist.

**Suggested remediation.** Either add the `GlobalState` account and the
`ensure_not_globally_paused` call to `liquidate_position` and `compute_funding` (closes
should arguably stay open while paused, which is a deliberate exception worth writing
down), or correct the comment at `mod.rs:169-170` to state which instructions are actually
covered. The comment and the code must not disagree about what the kill switch does.

### S3-X02 · `initialize_market` never validates `funding_interval`

- **Routed to:** S1
- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S1-C11
- **Evidence:** `programs/slipstream/src/instructions/initialize_market.rs:76`, `programs/slipstream/src/instructions/initialize_market.rs:85-92`, `programs/slipstream/src/instructions/initialize_market.rs:173`

**What is wrong.** This is the check half of S3-09's sibling finding S3-07, routed here
because `initialize_market.rs` is S1's file. `funding_interval` is parsed at line 76 and
written at line 173 with no validation in between, while the adjacent block at lines 85-92
rejects a zero `tick_size`, `lot_size` and `max_leverage` and an inconsistent fee pair. A
zero interval divides by zero in `compute_funding.rs:41`.

**Proof.** See S3-07 above for the pasted validation block and the divide site. The live
market is unaffected (`funding_interval_secs = 28800` in the account snapshot under
`docs/audit/audit-e2e/repro/s3/`), so this is an absent bound, not a live defect.

**Blast radius.** A market created with a zero interval has permanently unaccruable
funding.

**Suggested remediation.** Add `|| funding_interval == 0` to the condition at
`initialize_market.rs:85`.

### S3-X03 · The "Liq." column is not the price at which the program liquidates

- **Routed to:** S13
- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** S13-C1
- **Evidence:** `frontend/src/components/trading/positions-table.tsx:520-540`, `frontend/src/components/trading/positions-table.tsx:256`, `programs/slipstream/src/instructions/liquidate_position.rs:131-157`, `programs/slipstream/src/math/fixed_point.rs:117-138`, `README.md:220`

**What is wrong.** `liqAndHealth` takes the position's collateral as `_collateral` and
never reads it (`positions-table.tsx:525`), substituting `notional / MAX_LEVERAGE` on the
stated grounds that stored collateral is unreliable for old ER fills
(`positions-table.tsx:505-511`). The program's `compute_health_factor` uses
`pos.collateral` (`liquidate_position.rs:150-155`). The two also disagree on the
maintenance margin's price: the UI prices notional at `entry`
(`positions-table.tsx:528`), the program at the current mark
(`liquidate_position.rs:132`). The displayed number is therefore not the price where
health crosses 1.0, and `README.md:220` tells users the Positions table shows their
"liquidation price".

**Proof.** Solving both formulas for the crossing, 1 SOL long at $100 entry, 20×:

```
$ python3 -c '...'   # both formulas as written, see the record body
collateral == initial margin     UI liq $  97.50   program liq $  97.44   err -0.07%
2x margin posted                 UI liq $  97.50   program liq $  92.31   err -5.33%
margin eroded to $2              UI liq $  97.50   program liq $ 100.51   err +3.09%
```

The third row is the harmful direction: a position whose margin has been eroded by funding
or a partial close is already liquidatable (its crossing price is above the entry) while
the table still shows a comfortable $97.50. The UI number does not move with collateral at
all, so it cannot show this.

**Blast radius.** Every user sizing a position against the displayed number. On the live
deployment the error compounds with S3-03: the table says you are safe, the program
liquidates you, and your close button returns `OracleStale`.

**Suggested remediation.** Compute the crossing from the same inputs the program uses —
stored collateral, notional at the current mark, and accrued funding — and if collateral
from legacy ER fills is genuinely untrustworthy, render "—" for those positions rather
than a confident wrong number.

### S3-X04 · The only test over the funding catch-up path pins the premium to zero

- **Routed to:** S11
- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S11-C2
- **Evidence:** `tests/unit/src/test_compute_funding_regressions.rs:84-131`, `tests/unit/src/test_compute_funding_regressions.rs:109`, `tests/unit/src/test_liquidate_position_regressions.rs:58`, `tests/unit/src/test_liquidate_position_regressions.rs:185`

**What is wrong.** `test_compute_funding_accrues_all_missed_intervals` sets
`mkt.twap_prices[0] = price` with the same `price` fed to the Pyth stub, and the comment
at line 109 says why: "mark == index -> zero premium, isolates interval scaling". The
assertion is then `3 * INTEREST_RATE_PER_INTERVAL`. The multiplication that S3-01 turns
into a 908% index jump is therefore only ever run against a rate of zero, where any
multiplier is indistinguishable from any other. Separately, the two Mollusk tests over
`liquidate_position` stop at a forged-intent rejection and a healthy-position early
return; neither reaches the settlement branch, so nothing asserts on the liquidator's
balance (S3-04) or on the insurance-fund deficit path.

**Proof.** The whole suite passes and stays passing with both defects present:

```
$ SBF_OUT_DIR=$PWD/target/deploy cargo test --workspace
running 25 tests ... test result: ok. 25 passed; 0 failed
running 98 tests ... test result: ok. 98 passed; 0 failed
```

**Blast radius.** Two P0/P1 money-path defects sit under green tests. The assertion
quality, not the coverage count, is the gap: the interval test covers the line and proves
nothing about the value.

**Suggested remediation.** Add a case to
`tests/unit/src/test_compute_funding_regressions.rs` with a non-zero premium and a large
interval count, asserting a bound on the resulting index rather than an exact product; and
a `liquidate_position` case that runs to settlement and asserts both the liquidator's
credit and the insurance-fund movement.

## Cleared

- **S3-C1** — Rounding direction. Enumerated every division in the slice; all are Rust
  integer division, i.e. truncation toward zero, and all are bounded at 1 atom of error:
  `programs/slipstream/src/math/fixed_point.rs:24` (`mul_price`, floor),
  `:40` (`div_price`, floor), `:57` (`compute_notional`, floor -> notional understated ->
  margin and fee understated, favours the user), `:69` (`compute_initial_margin`, floor,
  favours the user), `:74` (`compute_maintenance_margin`, floor -> health overstated,
  liquidation delayed, favours the user), `:82` (`apply_bps`, floor -> fee and liquidator
  bonus understated), `:133` (`compute_health_factor`, floor -> health understated,
  favours the protocol), `:159` (`compute_vwap_entry`, floor),
  `programs/slipstream/src/math/funding.rs:90` (`compute_twap`, floor),
  `programs/slipstream/src/state/market.rs:133` (`get_twap`, floor),
  `programs/slipstream/src/instructions/close_position.rs:174` (proportional collateral
  release, floor -> the remainder stays in the position, neutral), and
  `programs/slipstream/src/instructions/liquidate_position.rs:192` (`net_collateral / 5`,
  floor). Three of these divide a value that can be **negative** —
  `programs/slipstream/src/math/fixed_point.rs:111` (PnL),
  `programs/slipstream/src/math/funding.rs:26` (premium) and
  `programs/slipstream/src/math/funding.rs:77` (payment) — where truncation toward zero is
  *asymmetric across the sign*: it always shrinks the magnitude, so a payer pays 1 atom
  less and a receiver receives 1 atom less. No round trip in the slice compounds this
  (each is a single division on an independent quantity), and 1 atom is $0.000001, so this
  is not a value leak. Recorded as the enumeration this class asks for, not as a finding.
- **S3-C2** — Precision loss. Covered by S3-08. Worked the 6-dp price x 9-dp size product
  at the stated 20x limit: `compute_notional` at
  `programs/slipstream/src/math/fixed_point.rs:53-62` promotes both operands to `u128`
  before multiplying, so a $91.32 x 87.4 SOL product (7.98e18) is nowhere near the u128
  ceiling and loses only the sub-atom remainder of the `/ 1e9`. The BASE_SCALE-vs-PRICE_SCALE
  hazard the file documents at `:6-12` is correctly applied at every one of its four call
  sites (`fixed_point.rs:57`, `funding.rs:65`, `liquidate_position.rs:132`,
  `fixed_point.rs:111`). `FUNDING_SCALE` at 1e18 gives the funding index 18 significant
  decimals, far more than the 6 the payment resolves to.
- **S3-C3** — Overflow and underflow. Covered by S3-05 (saturation) and S3-08 (the
  unchecked cast). Audited every arithmetic op in the slice: `checked_mul`/`checked_add`
  guard every widening multiply in `programs/slipstream/src/math/fixed_point.rs:21-23`,
  `:37-39`, `:54-56`, `:79-81`, `:108-110`, `:130-132`, `:148-150`, `:156-158` and
  `programs/slipstream/src/math/funding.rs:23-31`, `:55-57`, `:74-76`. The bare operators
  that remain are all provably bounded by a preceding guard:
  `programs/slipstream/src/instructions/claim_funding.rs:91` (guarded by the `>=` at
  line 90), `programs/slipstream/src/instructions/close_position.rs:210` and `:211`
  (guarded by `closing < abs_size` at `close_position.rs:141-145`),
  `programs/slipstream/src/state/market.rs:119` (guarded by the `< TWAP_BUFFER_SIZE` test
  at `:118`). `overflow-checks = true` is set at the workspace root (`Cargo.toml:13-14`),
  so a bare operator aborts rather than wrapping.
- **S3-C4** — i128 funding-index packing. Covered by S3-01. The lo/hi split itself is
  exact for the full i128 range in both directions:
  `programs/slipstream/src/state/market.rs:65-68` stores `value as i64` /
  `(value >> 64) as i64` and `:60-63` reconstructs with
  `(hi as i128) << 64 | (lo as u64 as i128)` — the `as u64` before the widening is what
  makes the zero crossing correct, and `programs/slipstream/src/state/position.rs:27-35`
  is byte-identical. Round-tripped the live values (`hi = -1`,
  `lo = -38671621383128857` reconstructing to `-38671621383128857`) in
  `docs/audit/audit-e2e/repro/s3/funding_catchup.py`. Wraparound is not reachable: at the
  documented 1 bps per 8h interval the index needs ~10^17 intervals to approach
  `i128::MAX`. A position that missed N periods settles correctly *by construction* — the
  diff at `programs/slipstream/src/math/funding.rs:55-57` is against the snapshot, not
  against a per-interval loop — which is precisely why S3-01's uncapped N is dangerous
  rather than merely inaccurate.
- **S3-C5** — Funding sign convention. Traced end to end and it is correct.
  `programs/slipstream/src/math/funding.rs:23` makes the premium positive when
  mark > index; `:66-70` signs the notional by position side; `:41-42` documents positive
  = pays. `programs/slipstream/src/instructions/claim_funding.rs:88-104` debits on
  positive and credits on negative, matching. `programs/slipstream/src/instructions/close_position.rs:190-191`
  subtracts it from the settlement, and `programs/slipstream/src/instructions/liquidate_position.rs:150-155`
  passes `-funding_payment` into `compute_health_factor`, whose parameter wants funding
  owed *to* the position — both negations are present and both are commented with the
  inverted-sign bug they fixed. Read
  `tests/unit/src/test_funding_sign_regressions.rs:48`, which covers the long-pays
  direction end to end through the real program; what it does *not* cover — the short
  side, the zero crossing of the index, and a non-zero premium — is routed as S3-X04.
- **S3-C6** — Health factor. Covered by S3-03. The formula at
  `programs/slipstream/src/math/fixed_point.rs:117-138` is
  `(collateral + uPnL + accrued_funding) * 1e6 / maintenance_margin` with three branches:
  zero maintenance margin (`:123-125`, see S3-08), non-positive net margin (`:127-129`,
  returns 0 = liquidatable, correct), and the normal path. Its inputs at
  `programs/slipstream/src/instructions/liquidate_position.rs:131-155` are the **live**
  dual-oracle price, not the frozen `last_mark_price` — verified against the live account,
  where a stale mark can therefore neither suppress nor trigger a liquidation. The threshold
  is 1.0 in 6-dp (`liquidate_position.rs:27`) and matches the definition written in
  `docs/08-glossary.md:69`.
- **S3-C7** — Liquidation price. The program neither stores nor emits one: no field on
  `Market` or `Position` (`programs/slipstream/src/state/position.rs:8-22`) and no
  computation in any of the 14 owned files. The only liquidation price in the product is
  computed client-side at
  `frontend/src/components/trading/positions-table.tsx:520-540`, it does **not** match
  where `compute_health_factor` crosses its threshold, and it is routed to S13 as S3-X03
  with the quantified error. Nothing is left in this slice's files for this class.
- **S3-C8** — Liquidation economics. Covered by S3-04. Also checked and found sound:
  liquidation is always **full**, never partial
  (`programs/slipstream/src/instructions/liquidate_position.rs:209-211` zeroes size, entry
  and collateral unconditionally), so there is no partial-liquidation griefing surface; a
  liquidator cannot profit from a healthy position because `:157-163` returns
  `HealthFactorAboveThreshold` before any settlement; and negative equity is explicitly
  absorbed rather than stranded (`:227-237`), with the ADL gap admitted in the comment at
  `:233-234`. The residual is that the absorber is a $20.73 insurance fund and that,
  per S3-04, nobody is paid to do the absorbing.
- **S3-C9** — `liquidation_intent` lifecycle. Sound. The intent is PDA-bound to the
  position at `programs/slipstream/src/instructions/liquidate_position.rs:269-275`, so a
  third party cannot execute one against a different position; there is no
  "execute someone else's intent" surface at all, because the intent only *permits* a
  liquidation that the health check at `:157-163` re-authorises from scratch on the second
  call. Prices cannot be frozen across the two phases: `initial_health_factor` is stored
  (`programs/slipstream/src/state/liquidation_intent.rs:21`) but never read back for a
  decision — the executing call recomputes health from a fresh oracle read. The
  `Ok(false)` returns at `:306` and `:315` are correct and the comment at `:253-259`
  documents the rollback bug that made them necessary. `close_liquidation_intent` at
  `:337-349` carries the owner, discriminator and position-match checks.
- **S3-C10** — Trigger orders. Covered by S3-06. The rest of the class is sound: execution
  is permissionless but the condition is re-checked at execution time against a *fresh*
  price (`programs/slipstream/src/instructions/execute_trigger.rs:80-86` uses
  `mark_price_for_close`, so a stale mark refuses to fire rather than firing at an old
  price), an unfired trigger is rejected with `TriggerConditionNotMet` at `:85`, and replay
  is impossible because `close_trigger_account` at `:93` zeroes the account in the same
  instruction that executes it. `place_trigger` validates kind, direction, non-zero price
  and position ownership at
  `programs/slipstream/src/instructions/place_trigger.rs:67-88` and is the only path that
  writes `trigger.owner`.
- **S3-C11** — `close_position` slippage bound. Covered by S3-09. When the bound is
  supplied it is enforced against the *actual* settlement price: `mark_price` is read once
  at `programs/slipstream/src/instructions/close_position.rs:123`, tested at `:129-138`,
  and the same variable is what `compute_unrealized_pnl` settles against at `:154` — there
  is no second read that could diverge. The direction is right for both sides (long sells,
  needs `mark >= limit`; short buys back, needs `mark <= limit`) and
  `tests/unit/src/test_close_position.rs:348` is a suite that exercises the rejection.
  `limit_price == 0` disabling the check is documented at `:22-27`, so it is a caller
  choice, not a bypass.
- **S3-C12** — `Market` parameter sanity. Covered by S3-07 (math consequence) and S3-X02
  (the check, routed to S1 —
  `programs/slipstream/src/instructions/initialize_market.rs:85-92`).
  The other parameters are safe for this slice's math:
  `max_leverage == 0` is rejected at
  `programs/slipstream/src/instructions/initialize_market.rs:85` *and* defended again at
  `programs/slipstream/src/math/fixed_point.rs:66-68`; `taker_fee_bps` is capped at 10,000
  and `maker_rebate_bps <= taker_fee_bps` at `initialize_market.rs:91`, which is what keeps
  `apply_bps` from paying out more than was collected; the maintenance margin is a fixed
  half of the initial margin (`fixed_point.rs:73-75`) rather than a stored parameter, so
  there is no value an operator can set that inverts the liquidation threshold. Live values
  read as `max_leverage = 20`, `taker_fee_bps = 6`, `maker_rebate_bps = 1`.
- **S3-C13** — `mark_price_for_close` (`programs/slipstream/src/state/market.rs:173-183`)
  and its three callers. Covered by S3-02 (the fourth, un-gated consumer) and S3-03 (the
  asymmetry). The three questions the class asks are answered, and the gate itself is
  **sound**: it returns `last_mark_price` when fresh, else `None`, falling back to the
  TWAP only before the first crank. The minute-quantised stamp *does*
  reject the frozen mark — verified live, `wrapping_sub` gives 23,213 minutes against a
  30-minute budget, so `is_mark_price_fresh` at
  `programs/slipstream/src/state/market.rs:154-161` returns false today. Modular arithmetic
  is correct for any age under 45.5 days, and the `stamp == 0` escape at `:157-158` is not
  reachable with a non-zero price, because `crank_twap` is the only writer of
  `last_mark_price` and it stamps on the adjacent line
  (`programs/slipstream/src/instructions/crank_twap.rs:71-76`), while
  `initialize_market.rs:178-181` zeroes both together. All three callers do the right thing
  on `None`: `close_position.rs:123-125`, `claim_funding.rs:61-63` and
  `execute_trigger.rs:81-83` each map it to `OracleStale` and abort before any state
  mutation.
